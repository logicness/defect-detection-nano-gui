#!/usr/bin/env bash
# =============================================================
# 兼容入口（2026-09-01）：VNC 桌面图标「启动上位机GUI.desktop」
# 仍指向本脚本，实际逻辑统一在 run_gui.sh（跟随 DISPLAY 启动）。
# 配合 main.py 全局单实例锁（127.0.0.1:43129）：无论从 HDMI 还是
# VNC 启动，后启动者自动激活已有窗口并退出，任意时刻只有一个 GUI。
# =============================================================
exec "$HOME/nano_gui/run_gui.sh" "$@"
